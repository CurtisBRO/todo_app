### AI interactive TODO database

This is a little learning project to demonstrate how tools are used by an LLM. The TODO is simple table which the LLM can call the tools to Create, Read, Update, or Delete a record.
It uses FastAPI, Python, Scaleway Managed SQL DB, and Scaleway's Generative API using the llama-3.3-70b-instruct LLM.
